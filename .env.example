# Esense Node Configuration
# Copiá este archivo a .env y completá los valores

# AI Provider: anthropic | claude_code | openai | ollama
# - anthropic: requiere ANTHROPIC_API_KEY (recomendado)
# - claude_code: usa el CLI de Claude Code instalado en el sistema (sin API key)
ESENSE_PROVIDER=anthropic
ANTHROPIC_API_KEY=

# OPENAI_API_KEY=

# Identidad del nodo (usá tu nombre, sin espacios ni mayúsculas)
ESENSE_NODE_NAME=yourname
ESENSE_DOMAIN=localhost

# Red
ESENSE_PORT=7777
ESENSE_DONATION_PCT=10

# URL pública — si no se setea, el nodo intenta levantar ngrok automáticamente
# ESENSE_PUBLIC_URL=https://abc.ngrok-free.app

# Bootstrap peer — DID de otro nodo para conectarse al arrancar
# ESENSE_BOOTSTRAP_PEER=did:wba:abc.ngrok-free.app:nombre
